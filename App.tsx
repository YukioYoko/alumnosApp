import React from 'react';
import Navigation from './navigation/Navigation'; // Asegúrate de que el archivo esté en la ruta correcta

const App = () => {
  return <Navigation />;
};

export default App;
